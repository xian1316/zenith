[package]
name = "zenith"
version = "0.1.0"
edition = "2021"  # This is just Rust's standard edition, it's fine

[dependencies]
sqlx = { version = "0.7", features = ["runtime-tokio-native-tls", "postgres"] }
tokio = { version = "1.0", features = ["full"] }
dotenv = "0.15"
anyhow = "1.0"